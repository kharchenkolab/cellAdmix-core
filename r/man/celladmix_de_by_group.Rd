% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{celladmix_de_by_group}
\alias{celladmix_de_by_group}
\title{Run DE Diagnostics Within Cell Subsets}
\usage{
celladmix_de_by_group(
  counts,
  cell_data,
  group_by,
  contrast = NULL,
  subset_by = NULL,
  subsets = NULL,
  cell_id_col = "cell_id",
  method = c("auto", "wilcox", "sccore"),
  min_cells_per_group = 3,
  normalize = c("cpm", "log1p_cpm", "none"),
  scale_factor = 10000,
  pseudocount = 0.1,
  p_adjust_method = "BH",
  sccore_low_expression_threshold = 0,
  sccore_auc = FALSE
)
}
\arguments{
\item{counts}{Gene-by-cell count matrix. Column names must be cell IDs.}

\item{cell_data}{Cell-level metadata data frame.}

\item{group_by}{Cell metadata column name, or named vector, defining the two
groups to compare within each subset.}

\item{contrast}{Two group labels. \code{logFC} is \code{contrast[1] / contrast[2]}.
If \code{NULL}, each subset must contain exactly two group labels.}

\item{subset_by}{Optional cell metadata column name, or named vector, used to
split cells before DE. If \code{NULL}, all cells are analyzed as one subset.}

\item{subsets}{Optional subset labels to analyze. If \code{NULL}, all non-missing
subset labels are used.}

\item{cell_id_col}{Cell ID column in \code{cell_data}. If absent, meaningful row
names are used.}

\item{method}{\code{"wilcox"} runs only \code{\link[=celladmix_de]{celladmix_de()}}. \code{"auto"} uses the same
DE test and appends sccore specificity metrics when \code{sccore} is available.
\code{"sccore"} requires successful sccore metric annotation.}

\item{min_cells_per_group}{Minimum number of cells required in each contrast
group within a subset.}

\item{normalize, scale_factor, pseudocount, p_adjust_method}{Passed to
\code{\link[=celladmix_de]{celladmix_de()}}.}

\item{sccore_low_expression_threshold}{Threshold used by
\code{sccore::appendSpecificityMetricsToDE()} when sccore metrics are appended.}

\item{sccore_auc}{Whether to request sccore AUC values. This also requires
the optional \code{pROC} package.}
}
\value{
A named list of DE tables, one per subset. The object has class
\code{celladmix_de_grouped}; call \code{summary()} on it to see subset sizes,
backend usage, and skipped subsets.
}
\description{
\code{celladmix_de_by_group()} is a convenience layer around \code{\link[=celladmix_de]{celladmix_de()}} for
common notebook diagnostics: compare two context groups within one or more
cell subsets. For example, in a tissue-domain analysis, one can compare
tumor-domain versus adjacent-normal-domain endothelial cells and immune cells
with a single call.
}
\details{
This function is intentionally not a replacement for a full single-cell DE
workflow. The statistical test is the lightweight \code{\link[=celladmix_de]{celladmix_de()}} Wilcoxon
path. When \code{method = "auto"} and \code{sccore} is installed, sccore specificity
metrics are appended to the returned DE tables; when \code{method = "sccore"},
sccore metric annotation is required and failures are treated as errors.
The p-values and logFC values still come from \code{\link[=celladmix_de]{celladmix_de()}} so results are
always returned in the same standardized format used by cellAdmix plotting
helpers.
}
