% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example_cells.R
\name{celladmix_select_example_cells}
\alias{celladmix_select_example_cells}
\title{Select Example Cells from a Score}
\usage{
celladmix_select_example_cells(
  score,
  rules = NULL,
  score_annotation = NULL,
  cell_data = NULL,
  targets = NULL,
  n_per_target = 4,
  p_thresh = 0.1,
  adjust_p = FALSE,
  use_rules = TRUE,
  min_molecules = 50,
  min_factor_molecules = 3,
  cells = NULL
)
}
\arguments{
\item{score}{A \link{CellAdmixScore} object.}

\item{rules}{Optional correction-rule table. If \code{NULL}, rules are generated
from \code{score} unless \code{use_rules = FALSE}.}

\item{score_annotation}{Optional score annotation object. If \code{NULL}, it is
generated from \code{score}.}

\item{cell_data}{Optional cell-level data frame with \code{cell_id},
\code{transcript_count}, and \code{dominant_factor} columns.}

\item{targets}{Optional target cell types to keep.}

\item{n_per_target}{Number of examples per target cell type.}

\item{p_thresh}{Score p-value threshold used when generating rules.}

\item{adjust_p}{Whether generated rules use adjusted p-values.}

\item{use_rules}{If \code{TRUE}, only factor/target combinations present in rules
are considered.}

\item{min_molecules}{Minimum target-cell molecule count.}

\item{min_factor_molecules}{Minimum score-table \code{factor_count} for a
target-cell/factor candidate.}

\item{cells}{Optional explicit target cell IDs. When supplied, selection is
restricted to these cells, the evidence filters are relaxed so every
requested cell is returned (with \code{NA} factor columns when the score table
has no evidence for it), and the cells come back in the requested order.}
}
\value{
Data frame of selected examples.
}
\description{
Selects target cells with strong score-supported non-native factor evidence.
Native factors are excluded using score source calls and the cell's dominant
factor; the displayed top admixture factor is then chosen by molecule
abundance inside the target cell, with score used as a tie-breaker.
The returned table can be passed to \code{\link[=celladmix_plot_cell_example]{celladmix_plot_cell_example()}} or
\code{score$plot_example()}.
}
