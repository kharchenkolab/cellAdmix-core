% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example_cells.R
\name{celladmix_prepare_cell_example}
\alias{celladmix_prepare_cell_example}
\title{Prepare an Example-Cell Plot}
\usage{
celladmix_prepare_cell_example(
  fit,
  example,
  score_annotation = NULL,
  cell_data = NULL,
  boundaries = "auto",
  stains = "auto",
  cell_types = "auto",
  markers = NULL,
  padding = 5,
  min_side = 24,
  max_pixels = 512
)
}
\arguments{
\item{fit}{A \link{CellAdmixFit} object.}

\item{example}{One row from \code{\link[=celladmix_select_example_cells]{celladmix_select_example_cells()}}.}

\item{score_annotation}{Optional score annotation used to identify native
factors from source calls.}

\item{cell_data}{Optional cell-level data frame with coordinates and factor
summaries. Defaults to \code{fit$cell_factors()}.}

\item{boundaries}{Cell boundaries used for contours and cell-type coloring.
The default \code{"auto"} discovers the standard Xenium boundary file next to
the source bundle (\code{NULL} when unavailable). Pass a boundary data frame or
path to override, or \code{NULL} to fall back to molecule hulls.}

\item{stains}{Stain images to compose as the plot background. The default
\code{"auto"} discovers the available Xenium stain images (DAPI and membrane)
from the fit's bundle and is empty for non-Xenium sources. Use \code{NULL} to
disable backgrounds, or pass a named list of stain-image descriptors to
override.}

\item{cell_types}{Cell-type labels used to shade cell polygons. The default
\code{"auto"} uses the dataset's active annotation; pass a named vector
(\code{cell_id} names, type values), a data frame with \code{cell_id}/\code{cell_type},
or \code{NULL} to disable shading.}

\item{markers}{Optional marker genes to size-emphasize inside the target cell.}

\item{padding, min_side}{Controls the square spatial window around the target
cell.}

\item{max_pixels}{Maximum stain-crop width/height.}
}
\value{
A list consumed by \code{\link[=celladmix_plot_cell_example]{celladmix_plot_cell_example()}}.
}
\description{
Prepare an Example-Cell Plot
}
