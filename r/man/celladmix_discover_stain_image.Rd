% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{celladmix_discover_stain_image}
\alias{celladmix_discover_stain_image}
\title{Discover a Xenium Stain Image}
\usage{
celladmix_discover_stain_image(
  run,
  stain = "membrane",
  image_path = NULL,
  focus_index = NULL,
  pixel_size = NULL,
  x_offset = 0,
  y_offset = 0
)
}
\arguments{
\item{run}{A \code{celladmix_run} object.}

\item{stain}{Stain name: \code{"membrane"}, \code{"dapi"}, or \code{"morphology"}.}

\item{image_path}{Optional explicit image path. Relative paths are resolved
relative to the Xenium bundle when possible.}

\item{focus_index}{Optional Xenium morphology-focus image index override.}

\item{pixel_size}{Optional microns-per-pixel override. If \code{NULL}, the Xenium
manifest pixel size is used.}

\item{x_offset, y_offset}{Optional physical-coordinate offset in microns.}
}
\value{
A list with \code{path}, \code{pixel_size}, \code{x_offset}, \code{y_offset}, and
\code{source}.
}
\description{
Resolves a named Xenium morphology-focus image and pixel size. The discovery
logic is intentionally soft: explicit \code{image_path} always wins; otherwise
known stain names map to conventional focus-image indices for current Xenium
multimodal bundles (\code{dapi} = 0, \code{membrane} = 1); \code{morphology} selects the
full morphology image from the manifest.
}
